# 외부 정찰 하네스 설계 기록

> 이 문서는 CTF 과제(“AI를 적극 활용한 CTF 풀이 및 보고서 작성”)를 위한 **외부 정찰 하네스**를
> 설계하면서 어떤 문제의식과 판단으로 접근했는지를 기록한 것이다. 코드가 무엇인지보다
> **왜 그렇게 설계했는지**에 초점을 둔다. 설계·검토 과정에서 AI(Claude)를 설계 파트너로 활용했고,
> 최종 판단과 방향 결정은 내가 했다.
>
> 작성자: 박중현

---

## 1. 과제 이해와 출발점

대상은 HTB ProLabs / Fortresses **스타일**의 **대규모 다중 플래그 엔터프라이즈 시뮬레이션 랩** CTF다.
HTB 자체를 푸는 게 아니라 그 스타일로 설계된 CTF이고, **실제 환경·네트워크 접근 방식·허용 대역은
CTF 당일 공개**된다. 즉 나는 환경을 모르는 상태에서 **미리 하네스를 만들어 두고 당일 바로 투입**해야
한다 — 이 제약이 설계 전반을 지배했다(특정 접속 방식이나 대역을 하드코딩하면 안 된다).
사전에 받은 힌트는 두 가지였다: **AWS 인프라가 쓰인다**는 것과 **정찰이 결정적으로 중요**하다는 것. 그리고 공격 벡터의 일부는 **취약한 오픈소스를 쓰되, CVE를 찾기 어렵게
버전 정보를 숨길 것**이라는 강사님의 언급이었다.

여기서 나는 두 개의 설계 축을 잡았다.

1. **정찰을 “HTTP 헤더 보기”로 끝내면 안 된다.** 버전을 숨긴다면 헤더의 `Server`/`X-Powered-By`는
   지워져 있을 것이다. 대신 페이지 접속 시 오가는 **패킷 — JS·CSS·쿠키·파비콘 같은 클라이언트
   자산**에 지문이 남는다. 특히 JS는 난독화돼 있을 수 있으니 **난독화 형태에 맞춰 정규화**한 뒤
   분석해 제품을 특정해야 한다. 강의에서 나온 “**코드 특징으로 제품 버전을 특정**”(예: React의
   버전별 API·내부 네이밍 차이)이라는 지식이 여기에 필요하다고 봤다.
2. **AI를 쓰되 AI가 거짓말하지 못하게 해야 한다.** 정찰 결과는 결국 보고서의 근거가 되는데,
   LLM은 CVE 번호나 버전 매핑을 아주 그럴듯하게 지어낸다. 이걸 구조로 막지 못하면 하네스 자체가
   신뢰를 잃는다.

## 2. 왜 “외부 정찰 하네스”를 먼저, 그리고 하네스를 여러 개로

하네스를 하나의 거대한 자동화로 만들지 않기로 했다. 대신 **국면별 하네스 여러 개**를 만들고,
**Claude Code가 오케스트레이터로서 “지금은 외부 정찰 국면 → 외부 정찰 하네스 실행”처럼 서브에이전트에
위임**하는 구조를 택했다. 이유:

- 랩은 다중 호스트·AD·피벗팅·AWS까지 얽힌다. 정찰/익스플로잇/보고를 한 덩어리로 묶으면 재사용도,
  검증도 어렵다.
- **정찰 안에서도 경계가 있다**: 이 하네스는 **외부 공격 표면**(웹·포트·vhost·서브도메인·공개 AWS)만
  본다. 발판을 잡은 뒤의 **내부 정찰**(내부망·AD 열거·피벗)은 성격도 스코프도 달라 **별도 하네스**로
  분리했다. 그래서 이름을 “정찰 하네스”가 아니라 **“외부 정찰 하네스”**로 못박았다.
- 서브에이전트는 콜드 스타트다(이전 맥락을 안 물려받는다). 그래서 **발견을 파일로 영속화**하고
  **하네스 간 인터페이스(출력 스키마)를 계약으로 고정**하면, 정찰 결과를 다음 하네스가 그대로
  입력으로 받을 수 있다.

정찰을 첫 번째로 고른 건, 정찰이 뒤 단계 전부의 입력을 만드는 **상류**이기 때문이다.

## 3. 기존 자산을 차용한 결정

밑바닥부터 만들지 않았다. 이전에 작업한 `cj-aisecurity-harness`(CVE 분석 하네스)가 이미
**“AI 출력은 Evidence가 아니다”**라는 계약과, 그것을 강제하는 **게이트 엔진**(해시 재계산으로 조작
증거 탐지, 근거 파일 실재 검증, `FACT`엔 출처 인용 강제, 스코프 위반은 STOP)을 갖고 있었다.

이건 내가 2번 축에서 원한 “환각 통제”와 거의 1:1로 대응했다. 그래서 **계약·게이트·증거 규율은
통째로 이식**하고, 성격이 다른 부분만 바꾸기로 했다:

- CVE 랩은 `localhost 전용`이지만 정찰은 **인가된 원격 대역(CTF가 당일 지정, 접근 방식 미정)**을
  다뤄야 한다 → 스코프 모델을 반대로 재정의하되 특정 대역·접속 방식은 하드코딩하지 않고 교체 가능하게.
- 단위가 `CVE`가 아니라 `타깃/자산` → 워크스페이스 구조 변경.
- **수동 정찰(그냥 접속)과 능동 정찰(스캔·브루트)을 분리** → 수동은 저위험이라 완화, 능동은
  `GO` 승인 게이트 유지.

## 4. 환각 통제를 “프롬프트 훈계”가 아니라 “구조”로

핵심 판단은 이거였다. **“조심해서 답해”는 소용없다. 스키마와 파이프라인이 강제해야 한다.**
그래서 여섯 개의 통제를 넣었다.

1. **증거 바인딩** — 버전/제품 단정은 반드시 실재 파일(`evidence_path`) + 그 파일의 실제 부분
   문자열(`evidence_quote`) + 재계산되는 해시(`asset_sha256`)로 묶는다. 근거가 없으면 스키마상
   주장이 불가능하다.
2. **출처 등급 강제** — 모든 발견은 `confirmed`/`inferred`/`guess`/`unknown`. **AI 추론은
   `confirmed`로 자가 승격할 수 없다.** 결정론 도구 매칭이나 재검증만 confirmed가 된다.
3. **CVE는 기억이 아니라 조회로** — 정찰 하네스는 “제품·버전”까지만 책임진다. CVE는 별도 하네스가
   외부 소스(NVD/GHSA/OSV) 조회로 처리한다. LLM의 기억 속 CVE는 신뢰하지 않는다.
4. **“모름”을 1급 출력으로** — `unknown`을 정식 값으로 두고, 판단 불가가 정답이면 그걸 뱉는 게
   성공이라고 계약에 명시. 환각은 “빠져나갈 구멍이 없을 때” 터지기 때문이다.
5. **2차 재검증** — 고가치 발견은 맥락을 제거하고 독립 재판정. 갈리면 강등.
6. **결정론적 게이트** — 위 전부를 사람이 아니라 코드가 검사(GO/REVISE/STOP)한다.

## 5. 게이트가 내 설계 버그를 잡은 사건 (증거 규율의 자기증명)

핑거프린터를 처음 돌렸을 때 게이트가 바로 REVISE를 냈다. 원인은 내가 만든 버그였다 — 근거를
정규화본 파일에 걸면서 해시는 원본 파일 걸 기록해, 둘이 안 맞았던 것이다. 게이트는 “해시가 실제
파일에서 재계산되지 않는다(fabricated)”고 정확히 지적했다.

이 순간이 설계의 정당성을 스스로 증명했다고 본다. **환각 통제 게이트는 AI(그리고 나)의 산출물도
똑같이 검증한다.** 그래서 “인용은 그 인용이 실제로 존재하는 파일(원본 우선)에 바인딩하고 그 파일을
해시한다”로 고쳐서 통과시켰다.

## 6. 한글 인코딩 — 해시가 아니라 “대조”가 문제였다

“한글 인코딩 때문에 해시가 안 맞아 게이트를 못 통과하는 사례”를 경계해야 했다. 파고들어 보니
해시 자체는 안전했다(양쪽 다 파일을 바이트로 읽어 해싱). **진짜 취약점은 `evidence_quote` 대조**
였다. 게이트가 파일을 UTF-8로 디코딩해 문자열 비교를 했는데, 한국 기업 앱 원본이 **CP949/EUC-KR**
이거나 **UTF-8 BOM**이면 파이썬(수집기)과 노드(게이트)의 디코딩이 어긋나 “인용이 없다”가 됐다.

실제로 이걸 검증하다가, 이 Windows의 기본 `open()` 인코딩이 CP949라 UTF-8 결과 파일을 그냥 읽으니
`UnicodeDecodeError`가 나는 장면까지 재현됐다 — 문제가 실재함을 눈으로 확인했다.

해결 원칙은 **“인코딩을 방정식에서 빼자”**였다:
- 게이트의 인용 대조를 UTF-8 디코딩이 아니라 **바이트 검색**으로 바꿨다.
- 수집·정규화는 견고한 디코더(UTF-8-BOM→UTF-8→CP949→latin-1)를 쓰고, 정규화본은 **표준 UTF-8/LF**로
  저장한다.
- 핑거프린터는 인용의 UTF-8 바이트가 실제로 들어있는 파일에 근거를 바인딩한다(게이트와 동일 기준).

CP949 한글 자산으로 end-to-end 테스트해서 이전 로직이면 REVISE였을 케이스가 GO가 되는 걸 확인했다.

## 7. SPA 대응 — 정적 수집의 사각지대

수집기를 정적으로만 두면 안 된다고 판단했다. 현대 SPA는 **빈 껍데기 HTML**을 주고 나머지를 런타임에
JS로 로드한다(webpack/vite 코드 스플리팅). 정적 파서는 런타임 청크·XHR·JS가 심는 쿠키를 못 본다.
그런데 **버전 지문의 대부분이 바로 그 런타임 청크 안**에 있다. 이건 강사님이 말한 “버전을 숨긴다”와
정확히 맞물린다 — 정적으로 안 드러나는 껍데기를 주면 페이지를 실제로 실행시켜야만 자산이 나온다.

그래서 헤드리스 Chromium(Playwright)으로 페이지를 구동해 **오가는 모든 응답 + 렌더된 DOM(런타임
지문 `__reactFiber$`·`data-reactroot` 포함)을 캡처**하는 수집기를 추가했다. 설계상 중요한 건
**같은 manifest 스키마로 출력**해서 정규화·핑거프린트·게이트를 한 줄도 안 바꾸고 재사용한 점이다.

## 8. vhost 대응 — 가상 네트워크라면 반드시

랩이 가상 네트워크를 만든다면 **한 IP가 Host 헤더로 여러 가상호스트를 서빙**하는 구성이 거의
확실하다고 봤다. 같은 IP라도 vhost마다 다른 앱·제품·버전이다. 그래서:

- **vhost 발견**(Host 브루트포스)은 타깃에 부하를 주는 능동 정찰 → `GO` 게이트로 분리.
- **vhost별 수집**은 “IP로 접속하되 올바른 Host/TLS SNI를 보내는” 능력이 필요 → SPA 수집기는
  Chromium `--host-resolver-rules`로, 정적 수집기는 Host 헤더 오버라이드로 대응하고, 워크스페이스를
  vhost 이름으로 키잉한다.

## 9. Docker 실행 모델 — 재현성이 곧 통제다

이 하네스를 돌리려면 로컬에 nmap·ffuf·chromium·파이썬 의존성을 깔아야 한다. 여기서 나는 **Docker
컨테이너에서 실행하고 결과를 로컬에 저장하는 형태**가 낫다고 판단했다. 이유는 단지 편의가 아니다:

- 이미지를 `@sha256`로 고정하면 **환경이 결정론적**이 된다 — 재현성 자체가 신뢰의 근거다.
- 컨테이너는 원본·해시를 **로컬 마운트 볼륨**에 쓰고, **호스트 쪽 게이트가 독립적으로 재검증**한다.
  컨테이너 안에서 무엇을 주장하든 호스트가 파일에서 다시 계산하니 **거짓말이 통하지 않는다.** 즉
  Docker화가 환각 통제와 같은 방향으로 작동한다.
- 실행은 **명령마다 껐다 켜는 방식이 아니라, 세션당 한 번 띄우고 `docker exec`로 명령을 투입하는
  장수 컨테이너**로 한다. 매번 재시작하면 무거운 도구 재부팅과 세션 상태·네트워크 접근 설정이
  유실되기 때문이다.

한 가지 중요한 정정: 처음엔 접속을 VPN으로 가정하고 설계에 그 흔적을 넣었는데, **CTF의 네트워크
접근 방식이 당일 공개되며 미정**임을 확인하고 걷어냈다. VPN(OpenVPN/WireGuard)·직접 라우팅·점프호스트
무엇이든 될 수 있으므로 **접근 방식은 교체 가능한 별도 프로파일**로 두고, 코어(수집·정규화·핑거프린트·
게이트)는 접근 방식과 무관하게 동작하도록 했다. 로컬 직접 실행도 폴백으로 남겼다. Windows Docker
Desktop 루프백 이슈도 주의로 기록.

## 10. 역할 분리 — 설계와 구현

여기서부터는 **전체 설계·계약·게이트는 확정하고, 실제 코드는 다른 코딩 에이전트에게 위임**하기로
했다. 그래서 남은 작업을 `TASK.md`에 콜드 스타트 전제의 자기완결적 지시서로 정리했다(각 태스크에
목적·이유·접근·설치할 도구·완료 기준·환각 통제 요구를 명시). 완료 기준을 “코드가 돈다”가 아니라
**“게이트가 검증한다”**로 못 박은 게 핵심이다.

## 10.5. 기존 오픈소스를 조사해 공백을 메우다

바퀴를 재발명하지 않기 위해, 성숙한 오픈소스 두 곳을 분석해 우리 설계와 대조했다.

- **cariddi**(Go 웹 크롤러/스캐너) — 깊이 크롤, JS·응답에서 **시크릿/토큰·엔드포인트·에러/스택트레이스
  regex 추출**, 응답 저장, JSON 출력. 여기서 우리 파이프라인의 **큰 공백**을 확인했다: 우리는 자산을
  수집·정규화하면서도 그 안의 시크릿·API 엔드포인트·에러페이지 버전 누출을 뽑지 않고 있었다.
  판단: 다시 만들지 말고 **cariddi를 도구로 통합하고 JSON 출력을 우리 finding 스키마로 파싱**하되,
  환각 통제를 위해 **"문자열 존재=confirmed / 그것이 진짜 시크릿·유효 엔드포인트인지=inferred"**로
  등급을 분리하고 시크릿은 마스킹한다(T15).
- **recon-skills**(AI 에이전트용 스킬 팩) — `AGENTS.md` + `validate_skills.py` 검증기 + 품질 기준
  문서 + `recon/auth/infra/chains/meta/redteam` taxonomy + MCP 통합. 놀랍게도 **우리의 AGENTS 계약 +
  게이트 + invariants 구조와 사실상 동형**이었다. 이것은 두 가지 의미다: (1) 우리가 독립적으로 도달한
  "계약+검증기" 설계가 실무 관행과 수렴한다는 정당성 근거, (2) 그들의 taxonomy가 우리 **멀티 하네스
  오케스트레이션**의 실제 조직 예시라는 것(정찰 하네스는 그중 `recon` 하나; `chains`(공격경로)·`meta`
  (계획)는 오케스트레이터 레벨). 코드로는 우리 공백인 **서브도메인/DNS 발견**(T17)을 반영했다.

교훈: 외부 도구는 "무엇을 통합할까"(cariddi)와 "설계가 옳은가를 대조"(recon-skills) 두 방식으로 쓴다.
두 경우 모두 통합·차용의 조건은 **우리 증거·provenance 규율에 맞게 등급화·검증**하는 것이다.

## 11. 현재까지의 결과와 남은 것

- **동작 확인**: 정적 수집 → 정규화 → 결정론 핑거프린트 → 게이트. 인코딩 견고성(CP949/BOM).
  SPA 동적 수집(브라우저 설치 시). vhost 수집. 스코프 STOP / 조작 REVISE / 정상 GO 3경로.
- **미구현(계약만 존재)**: AI fallback 추론, 2차 재검증 러너, vhost 발견·능동 정찰 러너, Docker
  환경, 시그니처 확장. → `TASK.md`로 이관.
- **정직하게 남기는 한계**: 라이브 SPA는 Playwright 미설치라 실기기 검증을 아직 못 했다.
  결정론 시그니처는 스타터 수준(React/jQuery)이다.

## 11.5. WSTG-INFO 커버리지 확장과 "테스트 격리" 교훈

이후 OWASP WSTG 정보수집(INFO) 항목으로 커버리지를 넓혔다(T23~T28): 메타파일(robots/sitemap/
security.txt/humans) 패시브 수집, 개발자 주석·내부 IP 추출, sitemap/robots 후보경로 파싱(수동)과
GO 게이트 fetch(능동), 인프라 헤더 지문. 여기서도 규율은 동일했다 — **존재는 confirmed, "민감한
누출인가"의 해석은 inferred/사람**. 특히 마스킹의 함정을 제대로 처리했다: 마스킹된 값은 원본 바이트에
없어 게이트 바이트 재검증에 걸리므로, **시크릿 앞 prefix를 evidence_quote로 바인딩하고 마스킹 전체값은
메타데이터로만** 두었다. "시크릿 비노출 + 근거는 바이트 검증 가능"을 양립시킨 것이다.

그리고 공개 배포 과정에서 **테스트 격리 결함**을 발견했다: 통합 테스트의 GO 케이스들이 저장소의
`scope.md`를 직접 읽어, 배포 기본값 `UNSET` 상태의 clean clone에서 `npm test`가 12건 실패했다
(게이트는 정상 — UNSET이니 REVISE가 맞다). 교훈은 명확하다. **테스트는 전역 상태(scope.md)에
의존하면 안 된다.** 게이트가 이미 `root` 인자를 받으므로 격리 루트를 넘겨 해결한다(신뢰 앵커 불변).
이는 "게이트가 정직하게 REVISE를 냈고, 정작 위생이 부족했던 건 테스트였다"는, 이 하네스 철학의
또 다른 자기증명이었다.

## 12. 이 설계에서 내가 지킨 원칙 한 줄 요약

**“AI는 판단에만, 근거는 파일에. 사람이 아니라 게이트가 검증한다. 모르면 모른다고 하는 게 성공이다.”**
