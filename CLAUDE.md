# Claude Code Adapter

이 저장소의 `AGENTS.md`를 먼저 읽고 공통 정본으로 따른다.
정책을 이 파일에 중복하지 않는다.
충돌하면 더 제한적이고 안전한 규칙을 적용한다.

## 오케스트레이터/서브에이전트 메모

- 이 저장소는 **외부 정찰(External Recon) 하네스**다(외부 공격 표면 전용 — 내부 정찰·포스트익스플로잇은 별도 하네스).
  오케스트레이터는 외부 정찰 국면에서 이 하네스를 서브에이전트에 위임한다.
- 서브에이전트는 콜드 스타트다. 발견은 반드시 `targets/<host>/findings.json`으로 영속화한다.
- 능동 정찰 실행 명령은 `GO <recon_id>` 형식만 승인으로 인정한다.
- 게이트: `npm run check -- <target>` → GO / REVISE / STOP.
